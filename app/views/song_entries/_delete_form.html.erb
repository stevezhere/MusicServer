<form action="/playlists/<%= @playlist.id %>/song_entries/destroy" method= "DELETE">
	<% @musics.each do |song| %>
		<input type="checkbox" name=song_entry[<%= song.id %>]><%= song.title %><br/>
	<% end %>
	<input type="submit" value="delete">
</form>