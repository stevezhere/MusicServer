<h1>RESULTS OF SCAN on Desktop</h1>

<ul>
	<% @files.each do |file| %>
		<li><strong><%= file.basename %>:</strong> <%= link_to file, musics_path('music' => {:title => file.basename}), method: :post%></li>
	<% end %>
</ul>

<%= link_to 'Back', new_music_path %>
