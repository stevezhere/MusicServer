<h1> Local Music Homepage </h1>

<ul>
	<% @musics.each do |music| %>
		<li><%= music.title %></li>
	<% end %>
</ul>

<%= form_for :music, method: :get, url: search_path do |f| %>
	<%= f.text_field :title %>
	<%= f.submit "Search" %>
<% end %>

<%= link_to "Add Songs", new_music_path %>
