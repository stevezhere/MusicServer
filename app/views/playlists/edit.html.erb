<h1> <%= @playlist.name %> Description </h1>
<%= form_for @playlist, method: :put do |f| %>
	<%= f.label :name %><br/>
	<%= f.text_field :name %><br/><br/>
	
	<%= f.label :description %><br/>
	<%= f.text_area :description %><br/>

	<%= f.submit 'Save Changes' %>

<% end %>