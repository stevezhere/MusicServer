<nav class="navbar navbar-default navbar-static-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">HOME</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">

        <li><%= render 'devise/menu/registration_items' %></li>
        <% if user_signed_in? %>
	        <li class="dropdown">
	          <a href="" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email %><span class="caret"></span></a>
	          <ul class="dropdown-menu">
	            <li><%= link_to 'Playlist Profile', user_path(current_user) %></li>
	            <li role="separator" class="divider"></li>
	            <li><%= link_to('Edit User', edit_user_registration_path) %></li>
	          </ul>
	        </li>
	      <% end %>
      </ul>
      <%= form_for :music, method: :get, url: search_musics_path, :html => {:id => "search", :class => "navbar-form navbar-left"} do |f| %>
      	<div class="form-group">
					<%= f.text_field :title, placeholder: "Enter Exact Song Title", :class => "form-control" %>
				</div>
				<%= f.button 'Search', :class => "btn btn-default" %>
			<% end %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= render 'devise/menu/login_items' %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>